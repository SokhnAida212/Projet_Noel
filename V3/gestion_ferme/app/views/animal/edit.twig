<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier l'Animal</title>
</head>
<body>
    <h1>Modifier l'Animal</h1>
    <form action="/animaux/{{ animal.id }}/update" method="POST">
        <label for="type">Type:</label>
        <input type="text" name="type" id="type" value="{{ animal.type }}" required><br>

        <label for="age">Âge:</label>
        <input type="number" name="age" id="age" value="{{ animal.age }}" required><br>

        <label for="sante">Santé:</label>
        <input type="text" name="sante" id="sante" value="{{ animal.sante }}" required><br>

        <label for="equipement_id">Équipement:</label>
        <select name="equipement_id" id="equipement_id">
            {% for equipement in equipements %}
                <option value="{{ equipement.id }}" {% if equipement.id == animal.equipement.id %} selected {% endif %}>{{ equipement.nom }}</option>
            {% endfor %}
        </select><br>

        <button type="submit">Mettre à jour</button>
    </form>
</body>
</html>